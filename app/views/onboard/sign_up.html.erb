<%= react_component("SignUpPage",
    props: {
      step: 1,
      inviteCode: @invite&.code,
      profilePictureUrl: @invite&.profile_picture_url,
      name: @invite&.name
    },
    prerender: false
) %>
